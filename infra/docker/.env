# Copy to .env next to docker-compose or export before compose up
POSTGRES_PASSWORD=dragtable_password
NEXT_PUBLIC_API_URL=
# DATABASE_URL=postgres://dragtable:dragtable_dev_change_me@postgres:5432/dragtable_control
